# waterius.github.io

Статические страницы Ватериуса на GitHub Pages.

Сайт отдаётся с корня репозитория (ветка `master`, папка `/`). Сборки нет:
файлы лежат как есть, `.nojekyll` отключает обработку Jekyll.
